# Gemfile
source "https://rubygems.org"

ruby "3.4.7"  # or match your installed Ruby version

# Testing framework (required by SWEBench private repo guideline)
gem "minitest", "~> 5.20"

# Optional: linting and formatting (for PR quality, not needed to run)
gem "rubocop", require: false
gem "rubocop-performance", require: false
gem "rubocop-minitest", require: false

# Optional: code coverage (enable only if you plan to report coverage)
gem "simplecov", require: false

# You do NOT need Rails, ActiveRecord, or external gems.
# The entire system runs with Ruby's standard library (JSON, Time, etc).
